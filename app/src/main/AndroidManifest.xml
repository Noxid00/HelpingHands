<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thisdoesnotexist.helpinghands">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CharityList" />
        <activity android:name=".NeighborhoodAlliance" />
        <activity android:name=".DonateToUs" />
        <activity android:name=".ConfirmDonation" />
        <activity android:name=".DonationComplete" />
        <activity android:name=".CharityDonation" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>
